/** Snapshot of Exchange widget fields for route comparison panel. */
export interface ExchangeIntentSnapshot {
  fromChain: string;
  toChain: string;
  fromToken: string;
  toToken: string;
  amount: string;
  recipient: string;
}
